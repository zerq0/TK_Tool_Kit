# Usage

Draft the commands you want your finished tool to support.

Examples:

```powershell
python -m portscanner_bnc --host 127.0.0.1 --ports 22,80,443
python -m portscanner_bnc --host scanme.nmap.org --ports 1-100
```

Only scan hosts you have permission to test.

